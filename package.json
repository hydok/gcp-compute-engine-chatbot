{
  "name": "gemini-web-chatbot",
  "version": "1.0.0",
  "private": true,
  "description": "로컬 PC에서 동작하는 Gemini 기반 웹 챗봇 (의존성 없음)",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": ">=20.12"
  }
}
